{
  "name": "@signet/native",
  "version": "0.1.0",
  "private": true,
  "description": "Signet native accelerators",
  "main": "index.js",
  "types": "index.d.ts",
  "files": [
    "index.js",
    "index.d.ts",
    "*.node"
  ],
  "napi": {
    "binaryName": "signet-native",
    "triples": {}
  },
  "scripts": {
    "build": "cp index.d.ts index.d.ts.bak && napi build --release --platform && mv index.d.ts.bak index.d.ts",
    "build:debug": "cp index.d.ts index.d.ts.bak && napi build --platform && mv index.d.ts.bak index.d.ts"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.5.1"
  }
}
